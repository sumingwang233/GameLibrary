import { LayoutGrid, ListFilter, RefreshCw, Search, SlidersHorizontal } from "lucide-react";
import { Input } from "./ui/input";
import { Button } from "./ui/button";

export function LibraryToolbar({ search, onSearch, sort, onSort, onRefresh, view, onView }: { search: string; onSearch: (value: string) => void; sort: string; onSort: (value: string) => void; onRefresh: () => void; view: string; onView: (value: string) => void }) {
  return <div className="mb-5 flex flex-wrap items-center gap-3"><div className="relative min-w-[220px] flex-1"><Search size={16} className="absolute left-3 top-1/2 -translate-y-1/2 text-text-secondary" /><Input value={search} onChange={event => onSearch(event.currentTarget.value)} placeholder="搜索游戏标题或路径" className="pl-9" aria-label="搜索游戏" /></div><div className="flex items-center gap-2"><SlidersHorizontal size={16} className="text-text-secondary" /><select value={sort} onChange={event => onSort(event.target.value)} className="h-10 rounded-md border border-border bg-[#121821] px-3 text-sm text-text-primary outline-none focus:border-steam" aria-label="排序"><option value="recent">最近入库</option><option value="title-asc">名称 A-Z</option><option value="title-desc">名称 Z-A</option></select><Button variant="outline" onClick={onRefresh} aria-label="刷新"><RefreshCw size={16} /></Button><div className="flex rounded-md border border-border p-1"><Button variant={view === "grid" ? "default" : "ghost"} className="min-h-8 px-2" onClick={() => onView("grid")} aria-label="网格视图"><LayoutGrid size={16} /></Button><Button variant={view === "compact" ? "default" : "ghost"} className="min-h-8 px-2" onClick={() => onView("compact")} aria-label="紧凑视图"><ListFilter size={16} /></Button></div></div></div>;
}
